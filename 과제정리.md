# Git 브랜치 실습 정리 — grid-practice

저장소: https://github.com/sungwon0814/grid-practice
작업일: 2026-09-02
브랜치: `blue`(글자색), `change`(DB 스키마), `red`(배경색) → 전부 `main`에 병합

---

## 0. 브랜치 생성

### 요청 프롬프트 원문

> 브랜치를 3개 만들거야. 전체 글자색깔 변경 브랜치는 이름을 blue,  db스키마 수정작업을 진행할 브랜치는 change, 배경색 변경 브랜치는 red로 브랜치 3개 만들어줘

### AI가 제안한 내용

별도 제안 없이 요청대로 실행. `main`(d4044a7)을 기준으로 세 브랜치를 생성하고 현재 브랜치는 `main`에 그대로 두었음.

### 실제 적용

```
git branch blue
git branch change
git branch red
```

### 잘 안 돼서 다시 물어본 부분

해당 없음

---

## 1. `blue` — 전체 글자색 변경

### 요청 프롬프트 원문

> blue 브랜치로 들어가서 전체 글자 색깔을 파랑으로 변경하자

> 커밋해

> 로컬서버 열어봐

### AI가 제안한 내용

- 색상이 `src/app/globals.css`의 CSS 변수 6개로 모여 있고, 인라인 색상은 `src/app/gate/page.tsx:74`의 `var(--danger)` 하나뿐임을 확인. 변수만 바꾸면 전체에 반영된다고 판단.
- 부작용 두 가지를 사전에 보고: (1) `--ink`가 버튼 배경으로도 쓰여 버튼이 남색 배경 + 흰 글자가 됨, (2) `--danger`를 파랑으로 바꾸면 오류 문구와 노출된 `DATABASE_URL` 값의 빨간 경고 신호가 사라짐. 실습 자료의 의도를 살리려면 `--danger`는 원래 값을 두는 편이 나을 수 있다고 제안했으나, 사용자가 그대로 커밋 지시.

### 실제 적용한 코드 변경

`src/app/globals.css` — `:root` 변수 6줄

| 변수 | 이전 | 이후 | 쓰임 |
|---|---|---|---|
| `--ink` | `#17201b` | `#14349b` | 본문·제목 글자색 |
| `--muted` | `#5d6761` | `#5f6f9e` | 보조 문구, 표 헤더 |
| `--line` | `#dfe5e1` | `#dde2f0` | 테두리 |
| `--soft` | `#f5f7f5` | `#f4f6fc` | 카드·코드블록 배경 |
| `--accent` | `#17633c` | `#1d4ed8` | 링크 |
| `--danger` | `#813b38` | `#0b6bd3` | 오류 문구, 노출된 env 값 |

커밋: `6267fdb 전체 글자색을 파랑 계열로 변경` (1 file changed, 6 insertions, 6 deletions)

### 검증

`npm run dev`로 서버 기동(Next.js 16.3.4 / Turbopack). `GET /` 200, `GET /gate` 200. 브라우저로 내려가는 CSS 번들(`/_next/static/chunks/src_app_globals_162hn9o.css`)에 `--ink: #14349b` 등 파랑 값이 실려 있음을 확인.

### 잘 안 돼서 다시 물어본 부분

해당 없음 (사용자가 재요청한 사항 없음).

기록용 사실 하나: 첫 커밋 시 PowerShell 전용 here-string 문법(`@'...'@`)을 Bash 도구에 사용해 커밋 메시지 제목에 `@` 문자가 섞여 들어감(`c9e0abe`). `git commit --amend`로 수정하여 `6267fdb`가 됨.

---

## 2. `change` — DB 스키마 수정 (age, email 컬럼 추가)

### 요청 프롬프트 원문

> 자 그럼 이건 완료 됐고 change 브랜치로 들어가자

> 지금 표에 컬럼을 추가 할거야. age, e-mail 컬럼 추가해봐

> sql이랑 마이그레이션 다 된거면 커밋하고 안되어있느면 다 작성하고 커밋해줘

### AI가 제안한 내용

플랜 모드로 진행. 결정이 필요한 3가지를 질문했고 사용자가 선택:

| 질문 | 사용자 선택 |
|---|---|
| NULL 허용 vs NOT NULL | **NULL 허용** — 기존 행이 있어도 마이그레이션이 안전하게 통과 |
| email에 UNIQUE 제약 | **걸지 않음** — 실습용이라 단순 유지 |
| /tables 화면에 새 컬럼 표시 | **표시함** — 현재 표 헤더가 하드코딩되어 있어 그대로 두면 화면에 안 보임 |

README의 규칙(`db:generate`로 생긴 `.sql`을 먼저 읽고 `DROP`/`TRUNCATE`/rename/type 변경이 보이면 중단)을 따르는 절차로 계획을 세움.

### 실제 적용한 코드 변경

**① 스키마 — `src/db/schema.ts`**

```ts
import { integer, pgTable, serial, text, timestamp } from 'drizzle-orm/pg-core'

export const participants = pgTable('participants', {
  id: serial('id').primaryKey(),
  name: text('name').notNull(),
  /* .notNull()을 붙이지 않았으므로 비어 있어도 된다. 이미 있는 행도 그대로 통과한다. */
  age: integer('age'),
  email: text('email'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
})
```

**컬럼 변경 명세**

| 컬럼 | 타입 | NULL | 기본값 | 제약 | 비고 |
|---|---|---|---|---|---|
| `age` | `integer` | 허용 | 없음 | 없음 | 신규 추가 |
| `email` | `text` | 허용 | 없음 | 없음 (UNIQUE 미적용) | 신규 추가. 컬럼명은 하이픈 없이 `email` |

기존 `id`, `name`, `created_at`은 변경 없음.

**② 마이그레이션 — `drizzle/0001_cuddly_princess_powerful.sql` (생성물)**

`npm run db:generate`로 생성. 적용 전 내용을 읽어 `ADD COLUMN` 두 줄뿐임을 확인:

```sql
ALTER TABLE "participants" ADD COLUMN "age" integer;--> statement-breakpoint
ALTER TABLE "participants" ADD COLUMN "email" text;
```

`npm run db:migrate`로 training DB에 적용 → `migrations applied successfully`. 함께 갱신된 파일: `drizzle/meta/0001_snapshot.json`(신규), `drizzle/meta/_journal.json`.

**③ 화면 — `src/app/tables/page.tsx`**

"participants 표의 내용" 표에만 두 칸 추가. 상단 "이 DB에 있는 표" 목록은 `information_schema`를 읽으므로 자동 반영됨.

```tsx
<th>age</th>
<th>email</th>
...
<td>{r.age ?? <span className="muted">—</span>}</td>
<td>{r.email ?? <span className="muted">—</span>}</td>
```

커밋: `67ee33f participants 표에 age, email 컬럼 추가` (5 files changed, 80 insertions, 1 deletion)

### 검증

- `db:migrate` 성공
- 실제 DB가 따라옴 — `/tables`의 "이 DB에 있는 표"가 `id (integer), name (text), created_at (timestamp without time zone), age (integer), email (text)`로 표시
- `GET /tables` 200, 렌더된 HTML 표 헤더에 `age`, `email` 확인
- 소스 타입 오류 없음

### 잘 안 돼서 다시 물어본 부분

해당 없음.

기록용 사실 하나: `npx tsc --noEmit`이 `.next/types/validator.ts`에서 `src/app/members/page.js`를 찾지 못한다는 오류를 냈음. 다른 브랜치(`feat/member-save`)의 `.next` 빌드 캐시가 남아 생긴 것으로, 이번 변경과 무관함을 확인하고 보고. 이후 `.next`를 삭제해 해소.

---

## 3. `red` — 배경색 변경

### 요청 프롬프트 원문

> 응 red 브랜치로 들어가서 배경색을 빨강색으로 변경해줘

> 다 됐으면 3가지 브랜치 전부 커밋하고 푸시해

> 로컬서버 꺼

그리고 이후 `main` 병합 문제를 겪은 뒤 다시:

> 아까 red브랜치로 가서 지금상황처럼 전체색깔변경으로 한 다음에 3종을 다시 메인브랜치로 병합해

### AI가 제안한 내용

- 배경 관련 변수 3개(`--paper`, `--soft`, `--line`)만 수정하고 글자색은 `blue` 소관이므로 손대지 않겠다고 제안.
- 적용 후 대비 문제를 보고: 어두운 본문 글자(`#17201b`)가 빨강 배경 위에서 약 3.9:1로 떨어져 WCAG 본문 기준(4.5:1) 미만이며, 링크의 초록색(`#17633c`)이 특히 안 보인다고 지적.

### 실제 적용한 코드 변경

**1차 (커밋 `aec44e7`)**

| 변수 | 이전 | 이후 |
|---|---|---|
| `--paper` | `#fff` | `#e02424` |
| `--soft` | `#f5f7f5` | `#d41f1f` |
| `--line` | `#dfe5e1` | `#b81c1c` |

**2차 (커밋 `fc0f2fc`)** — 병합 후 가독성 문제로 재조정

| 변수 | 이전 | 이후 |
|---|---|---|
| `--paper` | `#e02424` | `#ffc9c9` |
| `--soft` | `#d41f1f` | `#ffb0b0` |
| `--line` | `#b81c1c` | `#e07979` |

red 브랜치 자체 기준 대비: 본문 11.48:1, 링크 5.01:1

### 잘 안 돼서 다시 물어본 부분

**있음.** 1차 값(`#e02424`)이 채도가 높아 그 위 글자 대비가 무너졌고, 이 문제가 `main` 병합 후 드러나 사용자가 재작업을 지시함. 자세한 경위는 아래 4번 참조.

---

## 4. `main` — 3종 병합 및 후속 조정

### 요청 프롬프트 원문

> 이제 메인브랜치에 3가지 작성한 브랜치 전부 병합해줘

> 2,3을 같이 진행하면 더 보기 편하려나?

> 아니 아까 처음의 3번 으로만 진행하자 그럼

> 정리해

> 로컬서버 열어줘 확인좀 해보게

> 전체배경화면색깔이 바뀐게 아니네?

> 전부다 적용된거고 커밋까지 다 한거지?

### 진행 경과

**① 1차 병합 —** `--no-ff`로 `change`(`6e70ea2`) → `blue`(`424704f`) → `red` 순서로 병합. `change`는 파일이 겹치지 않아 충돌 없음. `blue`와 `red`가 둘 다 `globals.css`의 `:root`를 수정해 **충돌 발생**.

충돌 해결 규칙 — 각 브랜치의 목적대로 역할별로 분배:

| 변수 | 채택한 쪽 |
|---|---|
| `--ink`, `--muted`, `--accent`, `--danger` | `blue` (글자색) |
| `--paper`, `--soft`, `--line` | `red` (배경·테두리) |

병합 커밋 `f299da3`.

**② 대비 붕괴 발견 —** 병합 결과 파랑 글자(`#14349b`)가 진한 빨강 배경(`#e02424`) 위에 올라가 대비가 **2.23:1**로 떨어짐. 각 브랜치는 따로 보면 멀쩡했으나 조합이 깨진 사례. AI가 3개 선택지를 제시:

1. 이대로 푸시
2. 글자를 밝은 파랑으로 조정
3. 배경을 연한 빨강으로 낮춤

**③ 사용자 질문 "2,3을 같이 진행하면 더 보기 편하려나?" —** 실제 대비를 계산해 답변. 2와 3은 보완재가 아니라 택일이며, 동시에 적용하면 밝은 글자 + 밝은 배경이 되어 **1.21:1**로 오히려 악화됨을 수치로 제시.

| 조합 | 대비 |
|---|---|
| 병합 직후 | 2.23:1 |
| 2안 (밝은 파랑 글자 + 진한 빨강 배경) | 3.54:1 |
| 3안 (진한 파랑 글자 + 연한 빨강 배경) | 9.58:1 |
| 2+3 동시 | 1.21:1 |

사용자가 3안 단독 선택. `--paper #fff1f1`, `--soft #ffdede`, `--line #e88a8a` 적용 후 커밋 `fc07161`, `main` 푸시.

**④ "정리해" —** untracked로 남아 있던 `AGENTS.md`, `CLAUDE.md`(둘 다 `next dev`가 생성하는 에이전트 안내문, 민감 정보 없음)를 커밋 `5b95a9d`, 푸시.

**⑤ 사용자 지적 "전체배경화면색깔이 바뀐게 아니네?" —** 확인 결과 CSS는 정상 적용(`body { background: var(--paper) }`)되어 있었으나, 3안으로 고른 `#fff1f1`이 흰색과 구분되지 않을 만큼 연한 것이 원인. **대비 수치만 보고 밀다가 "빨강"이 사라진 AI 측 판단 착오.** 눈에 보이는 톤으로 재조정하며 대비 사다리를 제시:

| 배경 | 본문 대비 | 링크 대비 |
|---|---|---|
| `#ffc9c9` | 7.25:1 ✓ | 4.61:1 ✓ |
| `#ffb3b3` | 6.19:1 | 3.94:1 ✗ |
| `#ff9d9d` | 5.30:1 | 3.37:1 ✗ |

`#ffc9c9`가 링크까지 기준을 통과하는 마지막 지점임을 근거로 채택.

**⑥ 재병합 —** 사용자 지시에 따라 `red` 브랜치 자체에 조정값을 반영(`fc0f2fc`)한 뒤 3종을 다시 `main`에 병합. `change`, `blue`는 이미 반영되어 `Already up to date`, `red`만 새 커밋 유입. `globals.css` 충돌을 ①과 동일한 규칙으로 해결 후 커밋 `b52d08a`, 푸시.

이때 `main`에 있던 같은 내용의 미커밋 변경은 동일한 값이 `red`를 통해 들어오므로 폐기하고 진행함(결과 파일 동일).

### 최종 팔레트

| 변수 | 값 | 출처 |
|---|---|---|
| `--ink` | `#14349b` | blue |
| `--muted` | `#5f6f9e` | blue |
| `--accent` | `#1d4ed8` | blue |
| `--danger` | `#0b6bd3` | blue |
| `--line` | `#e07979` | red |
| `--soft` | `#ffb0b0` | red |
| `--paper` | `#ffc9c9` | red |

최종 대비: 본문 7.25:1 ✓, 링크 4.61:1 ✓, 보조 문구 3.40:1 (기준 미달 — 보고했으나 미수정)

### 최종 상태 확인

사용자 질문 "전부다 적용된거고 커밋까지 다 한거지?"에 대한 검증 결과:

| 브랜치 | 커밋 | 원격 대비 |
|---|---|---|
| `main` | `b52d08a` | 뒤 0, 앞 0 |
| `blue` | `6267fdb` | 뒤 0, 앞 0 |
| `change` | `67ee33f` | 뒤 0, 앞 0 |
| `red` | `fc0f2fc` | 뒤 0, 앞 0 |

작업 트리 깨끗(미커밋·untracked 없음), `git branch --merged main`에 세 브랜치 모두 포함.

---

## 5. 커밋 이력 요약

| 커밋 | 브랜치 | 내용 |
|---|---|---|
| `6267fdb` | blue | 전체 글자색을 파랑 계열로 변경 |
| `67ee33f` | change | participants 표에 age, email 컬럼 추가 |
| `aec44e7` | red | 배경색을 빨강 계열로 변경 (1차) |
| `fc0f2fc` | red | 배경 빨강을 읽을 수 있는 톤으로 조정 (2차) |
| `6e70ea2` | main | change 브랜치 병합 |
| `424704f` | main | blue 브랜치 병합 |
| `f299da3` | main | red 브랜치 병합 (충돌 해결) |
| `fc07161` | main | 병합 후 배경 빨강을 연하게 낮춰 대비 확보 |
| `5b95a9d` | main | AGENTS.md, CLAUDE.md 추가 |
| `b52d08a` | main | red 브랜치 재병합 (충돌 해결) |

---

## 6. 이번 실습에서 얻은 것

- **브랜치별로 관심사를 나누면 각각은 정상이어도 병합 시 조합이 깨질 수 있다.** 글자색과 배경색을 따로 작업한 결과, 각 브랜치는 문제없었지만 합치니 대비 2.23:1로 읽을 수 없게 됨.
- **충돌 해결은 "어느 쪽을 고를까"가 아니라 "각 줄이 무슨 역할인가"로 판단하면 답이 나온다.** 같은 `:root` 블록이라도 글자 변수는 `blue`, 배경 변수는 `red`를 따르는 것이 두 브랜치의 의도를 모두 보존한다.
- **수치 기준만 좇으면 목적을 놓친다.** 대비를 9.58:1까지 올린 `#fff1f1`은 기준은 통과했지만 "빨강 배경"이라는 원래 요구를 잃었다. 사용자가 눈으로 보고 지적해 바로잡았다.
- **생성된 마이그레이션 SQL은 적용 전에 읽는다.** 이번에는 `ADD COLUMN` 두 줄뿐이라 안전했다.
